function helloWorld(params) {
    if (params === void 0) { params = 0; }
    var hello = "Hello";
    var world = "world";
    return hello + world;
}
function helloWorldTs(params) {
    if (params === void 0) { params = 0; }
    var hello = "Hello";
    var world = "world";
    return hello + world;
}
